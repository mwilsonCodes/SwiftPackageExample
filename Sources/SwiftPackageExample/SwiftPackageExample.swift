public struct SwiftPackageExample {
    public private(set) var text = "Hello, World!"

    public init() {
    }

    public func add(num1: Int, num2: Int) -> Int {
        return num1 + num2
    }

    public func sub(num1: Int, num2: Int) -> Int {
        return num1 - num2
    }
}
